<p style="color: green"><%= notice %></p>

<%= render @route %>

<div>
  <%= link_to "Edit", edit_route_path(@route) %> |
  <%= link_to "Back", routes_path %>

  <%= button_to "Delete", @route, method: :delete %>
</div>
