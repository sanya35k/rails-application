<%= form_with(model: [:admin, railway_station], local: true) do |form| %>
  <% if railway_station.errors.any? %>
    <%= render 'common/error', resource: railway_station %>
  <% end %>

  <div class="field">
    <%= form.label t('.title') %>
    <%= form.text_field :title %>
  </div>

  <div class="actions">
    <%= form.submit t('.submit', action: railway_station.new_record? ? 'Создать' : 'Редактировать') %>
  </div>
<% end %>
